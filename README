TRASH and RUMMAGE

Support:
POSIX.

Installation:
Type `make install` to install. The binaries are moved to ~/bin.
Type `make uninstall` to uninstall.
Run some tests by typing `make test`.

Usage:
Type `trash <filenames>` to move unwanted files to the /tmp/trash directory.

Type rummage to list files in the trash. Type rummage <filename> to retrieve
the file.
